<!DOCTYPE html>
<h1>tasks</h1>

<ul>
  <% @tasks.each do |task| %>
    <li><%= link_to task.title, task_path(task) %></li>
    <%= link_to 'Remove', new_task_path(@task), method: :delete  %>
  <% end %>
</ul>

# <%= link_to 'New Task' , new_task_path %>
